import SwiftUI

@main
struct PassengerApp: App {
    var body: some Scene {
        WindowGroup {
            ContentView()
        }
    }
}

/// Empty on purpose — the first PRD decides what goes here.
struct ContentView: View {
    var body: some View {
        Color.clear
    }
}
